# 要件: テスト規約の追加

## 目的

`docs/development-guidelines.md` の `## テスト規約` セクション（現状は「今後追記」のプレースホルダー）を実装する。

## 方針

- **新規ルールの発明ではなく、既存テストコードの規約を明文化する**
- 配置先は `development-guidelines.md` に追記（分離しない）
- プロダクションコード・テストコードへの変更なし

## スコープ

- `docs/development-guidelines.md` のみ変更
- `src/` 配下は変更しない

## 制約

- 既存のコーディング規約・Git 規約セクションのスタイルと統一する
- 表・コードブロック・見出し階層を揃える
