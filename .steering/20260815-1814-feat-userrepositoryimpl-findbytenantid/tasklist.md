# tasklist

- [x] 作業対象を実装する（`infrastructure/persistence/repository/UserRepositoryImpl#findByTenantId(TenantId)` の本体。`userJpaRepository.findByTenant_IdOrderByIdAsc(tenantId.value()).stream().map(userMapper::toDomain).toList()` を返し、`// TODO` コメントを削除する。あわせて実装手段として `infrastructure/persistence/repository/UserJpaRepository` へ `@EntityGraph(attributePaths = "tenant")` 付きの派生クエリ `List<UserJpaEntity> findByTenant_IdOrderByIdAsc(Long tenantId)` を追加する（`java.util.List` / `org.springframework.data.jpa.repository.EntityGraph` の import 追加）。並び順は SQL の `ORDER BY id ASC` で保証し Java 側で `sorted()` しない。`is_active` による絞り込みは行わない。`domain/repository/UserRepository`・`User`・`UserMapper`・`UserJpaEntity`・`TenantJpaEntity`・application 層・presentation 層・`config/`、および `findByTenantCodeAndEmail` / `findById` / `findByTenant_CodeAndEmail` は変更しない）
- [x] Javadoc を記載する（`UserRepositoryImpl#findByTenantId` の「現時点では未実装のスタブであり、常に空リストを返す。」を削除し、`UserJpaRepository#findByTenant_IdOrderByIdAsc` へ委譲して `UserMapper` でドメインモデルへ変換すること・ID 昇順は SQL の `ORDER BY` で保証し Java 側で並び替えないこと・無効ユーザー／無効テナントのユーザーも除外しないこと（有効性判定は呼び出し元の責務）・`@EntityGraph` による `tenant` の JOIN FETCH で N+1 を回避していることを明記し、`@param` / `@return`（該当なしは空リスト）を記載する。`UserJpaRepository#findByTenant_IdOrderByIdAsc` にも Javadoc を付け、クラス Javadoc をテナント ID 検索を含む記述へ更新する。新規テストクラス `UserRepositoryImplTest` にもクラス Javadoc を付ける）
- [x] 正常系のテストコードを記載する（新規 `src/test/java/io/github/aki0057/multitenant/auth/infrastructure/persistence/repository/UserRepositoryImplTest.java` を `@DataJpaTest` + `TestEntityManager` で作成し、`@BeforeEach` で `new UserRepositoryImpl(userJpaRepository, new UserMapperImpl())` を手動配線する。`findByTenantId_found()`（全件返却と各フィールドの変換）／`findByTenantId_orderedByIdAsc()`（永続化順と辞書順が一致しないデータで `userId` 昇順を検証）／`findByTenantId_includesInactiveUser()`（`is_active = false` も含まれ `userIdIsActive()` が `false`）／`findByTenantId_inactiveTenant()`（無効テナントでも返り `tenantIdIsActive()` が `false`）／`findByTenantId_otherTenantExcluded()`（同一メールアドレスを含む 2 テナントで指定テナントのみ返る）。あわせて既存 `UserJpaRepositoryTest` へ `findByTenant_IdOrderByIdAsc_found()` と `findByTenant_IdOrderByIdAsc_includesInactiveUser()` を追記する（既存テストケース・既存ビルダーは変更せず、必要なら `buildUser` / `buildTenant` のオーバーロードを Javadoc 付きで追加する）。命名は `methodName_condition()`、`@DisplayName` は日本語で先頭に `正常系:` を付ける）
- [x] 異常系のテストコードを記載する（`UserRepositoryImplTest` に `findByTenantId_noUsers()`（ユーザーが 0 人のテナントで `null` ではなく空リスト）と `findByTenantId_unknownTenantId()`（`new TenantId(Long.MAX_VALUE)` で例外をスローせず空リスト）を追加し、既存 `UserJpaRepositoryTest` へ `findByTenant_IdOrderByIdAsc_notFound()`（該当なしで空リスト）を追記する。`TenantId` の `null` / 0 以下は VO 生成時に弾かれ Repository には到達しないため対象外である旨をコメントで残してよい。`@DisplayName` は日本語で先頭に `異常系:` を付ける）
- [x] 今回実装した作業対象に対応する TODO.md のチェックボックスを埋める（`## Repository (domain)` の `- [ ] findByTenantId（UserRepository）` と `## infrastructure.persistence` の `- [ ] UserRepositoryImpl#findByTenantId を実装` の 2 件を完了にする。他の項目は変更しない）
