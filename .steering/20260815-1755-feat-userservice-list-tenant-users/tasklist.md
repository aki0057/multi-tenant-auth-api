# tasklist

- [x] 作業対象を実装する（`application/UserService#listTenantUsers(ListTenantUsersCommand)` の本体。入口で `new TenantId(command.tenantId())` へ変換し `IllegalArgumentException` は握りつぶして `List.of()` を返す。`userRepository.findByTenantId(tenantId)` の結果を `filter` / `sorted` せず `stream().map(user -> new TenantUserResult(user.userId().value(), user.email().value(), user.role().value(), user.userIdIsActive())).toList()` で 1:1 に詰め替えて返す。`@Transactional(readOnly = true)` は維持し、スタブ実装時の `// TODO` コメントを削除する。あわせて内側スタブとして `domain/repository/UserRepository` へ `List<User> findByTenantId(TenantId tenantId)` を追加し、`infrastructure/persistence/repository/UserRepositoryImpl#findByTenantId` を `// TODO` + `List.of()` のスタブとして作成する。`UserJpaRepository`・`User`・`UserMapper`・presentation 層・`ListTenantUsersCommand` / `TenantUserResult`・`UserService#getMe`・`config/SecurityConfig` は変更しない）
- [x] Javadoc を記載する（`UserService#listTenantUsers` の既存 Javadoc を整え、`isActive` に詰めるのは `users.is_active` 単体（`User#userIdIsActive()`）で `User#isActive()` ではないこと・テナントの有効性は判定せず無効ユーザー／無効テナントを除外しないこと・テナント越え防止は Repository のクエリ条件で担保すること・並び順（ID 昇順）の保証責務は `UserRepository#findByTenantId` にあり本メソッドは順序をそのまま維持すること・`tenantId` の VO 検証失敗時は例外を投げず空リストを返すことを明記する。`UserRepository#findByTenantId` の Javadoc には「指定テナントのユーザーをすべて返す（無効ユーザーも除外しない）」「ID 昇順で返す」「該当なしは空リスト」を明記する。`UserRepositoryImpl#findByTenantId` は `// TODO` 付きのスタブである旨を記す）
- [x] 正常系のテストコードを記載する（`src/test/java/io/github/aki0057/multitenant/auth/application/UserServiceTest.java` へ追記。`listTenantUsers_success()`（複数件が Repository の返した順序＝ID 昇順のまま 1:1 で詰め替えられる）／`listTenantUsers_convertsTenantIdToValueObject()`（`verify(userRepository).findByTenantId(new TenantId(1L))`）／`listTenantUsers_includesInactiveUser()`（`userIdIsActive = false` が除外されず `isActive = false` で含まれる）／`listTenantUsers_inactiveTenantUser()`（`tenantIdIsActive = false` でも `isActive` は `users.is_active` 単体の値）／`listTenantUsers_empty()`（Repository が空リストなら空リスト）。`@ExtendWith(MockitoExtension.class)` + `@Mock UserRepository` + `@InjectMocks UserService` の既存構成を流用し、既存 `getMe` のテスト・フィクスチャは変更しない。必要なら `userId` を差し替えられる補助メソッドを Javadoc 付きで追加する）
- [x] 異常系のテストコードを記載する（`listTenantUsers_invalidTenantId()`（`tenantId` が 0 以下）／`listTenantUsers_nullTenantId()`（`tenantId` が `null`）で、例外をスローせず空リストを返し `verify(userRepository, never()).findByTenantId(any())` であることを検証する。命名は `methodName_condition()`、`@DisplayName` は日本語で先頭に `異常系:` を付ける）
- [x] (条件: 作業対象が ValueObject でも Repository でもない場合) 内側レイヤーに作成したスタブを TODO.md に空のチェックボックスで追記する（`## Repository (domain)` の章へ `findByTenantId（UserRepository）`、`## infrastructure.persistence` の章へ `UserRepositoryImpl#findByTenantId を実装`）
- [x] 今回実装した作業対象に対応する TODO.md のチェックボックスを埋める（`## Service (application)` の `- [ ] UserService#listTenantUsers(ListTenantUsersCommand)` を完了にする）
